services:
  - type: web
    name: schemavcs
    env: python
    plan: free
    buildCommand: "pip install poetry && poetry install --no-root && poetry install"
    startCommand: "poetry run uvicorn schemavcs_web.app:app --host 0.0.0.0 --port $PORT"
